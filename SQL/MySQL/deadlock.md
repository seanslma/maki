# Deadlock


```sql
--to see the last deadlock in an InnoDB user transaction
SHOW ENGINE INNODB STATUS

--enabled setting to print information about all deadlocks to the mysqld error log
innodb_print_all_deadlocks
```